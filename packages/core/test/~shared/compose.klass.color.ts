export const options = {
  base: "color-base",
  variants: {
    color: {
      red: "color-red",
      green: "color-green",
      blue: "color-blue",
    },
  },
} as {
  base: string;
  variants: {
    color: {
      red: string;
      green: string;
      blue: string;
    };
  };
};
